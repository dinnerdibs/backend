package com.dinnerdibs.backend.config.api;

/**
 * A helper class to centralize the backend's API versioning structure
 */
public class ApiVersions {
    /**
     * A private static field for the first API version
     */
    private static final String API_VERSION_ONE = "v1";

    /**
     * Static field to set the current stable API version for the system
     */
    public static final String CURRENT_API_VERSION = API_VERSION_ONE;
}

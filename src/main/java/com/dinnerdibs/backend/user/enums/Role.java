package com.dinnerdibs.backend.user.enums;

/**
 * Enumeration type for the User account's role
 * A User can have multiple roles at a given time
 */
public enum Role {
    CUSTOMER,
    CHEF,
    ADMIN
}
